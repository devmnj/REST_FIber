{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:RSET_GORM}/test.db",
            "name": "sqlite"
        }
    ]
}